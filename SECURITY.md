# 安全政策（Security Policy）

## 支持的版本

我们仅为最新发布版本提供安全修复。升级前请先查阅 [CHANGELOG.md](CHANGELOG.md) 确认版本状态。

| 版本 | 是否支持 |
| --- | --- |
| 最新 release | ✅ |
| 更早版本 | ❌（请先升级） |

## 如何报告安全漏洞

**请勿通过公开 GitHub Issue 报告安全漏洞。**

请使用以下任一私密渠道：

1. **GitHub 私有安全公告（首选）**：<https://github.com/13861419/dockerDesktop/security/advisories/new>
2. **仓库私聊/邮件**：维护者联系方式见仓库主页

报告时请尽量包含：

- 受影响的版本与部署形态（Windows / Linux、NSSM / 手工部署）
- 复现步骤或概念验证（PoC）
- 影响面评估（数据泄露 / 权限提升 / 拒绝服务等）

## 响应承诺

| 阶段 | 时限 |
| --- | --- |
| 确认收到 | 48 小时内 |
| 严重度评估与初步反馈 | 7 天内 |
| 修复发布 | 高危尽快（临时缓解措施先行），中低危随下个版本 |

## 范围界定

**属于本项目的安全问题的例子：**

- 面板鉴权/会话/权限（RBAC、审计员只读、审批流）绕过
- SQL 注入、XSS、SSRF、命令注入等注入类漏洞
- 敏感信息（密钥、密码哈希、凭证）泄露
- 安全默认配置缺陷（如默认口令、鉴权缺失）

**不属于本项目范围（不予处理）：**

- 对默认账号 `admin / admin888` 的暴力破解——首次登录会强制改密，且部署者应立即修改；配合登录锁定与[安全入口](docs/DockerManager-操作手册.md)（`ENTRANCE_PATH`）可缓解
- 需要面板宿主机本地执行权限的攻击（拥有本地执行权者本可直接读取数据库与密钥文件）
- 针对被管理 Docker 引擎/K8s 集群自身（非面板）的漏洞
- 社会工程学攻击

## 安全加固建议（部署者）

1. 首次部署后立即修改默认密码，并为管理员开启两步验证（2FA）
2. 公网部署时设置 `ENTRANCE_PATH` 隐藏入口路径
3. 使用 IP 白名单（用户级或全局）限制管理端访问来源
4. 修改默认服务端口（9528），并置于反代/防火墙之后

## 致谢

报告者可选择在修复公告中公开署名。感谢每一位负责任披露的安全研究者。
