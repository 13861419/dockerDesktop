# 更新日志（CHANGELOG）

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.9.0] - 2026-09-02

本版本为 **K8s 指标持久化**：节点资源快照定时采样落库，接入 90 天小时级聚合与长周期趋势曲线。

### Added（新增）

- **K8s 节点指标采样器**：`k8s/metrics.ts` 每 60 秒经 metrics-server 采集各节点 CPU（核）与内存（字节）写入 `k8s_metrics` 原始表（保留 7 天）；K8s 不可用时静默跳过
- **小时级聚合**：复用 metrics_hourly 机制（scope='k8s-node'，key=节点名），rollup 定时器自动聚合，保留 90 天
- **曲线端点**：`GET /api/k8s/metrics-history?duration=1d|7d|30d|90d` 返回集群级 CPU（毫核）/内存（KiB）聚合曲线
- **前端**：集群概览新增「节点资源趋势」卡片（时间窗切换 + 双曲线），采样器运行约 1 小时后出图

### Test（测试）

- 新增单测 k8sMetrics.test.ts（2 例）：节点采样小时级聚合（多节点分组）与集群求和查询；单测累计 **298/298**
- 假 apiserver 冒烟 3/3（曲线端点结构 + 概览回归）；E2E 5/5；docs:check 86 图 0 缺失

## [1.8.0] - 2026-09-02

## [1.8.0] - 2026-09-02

本版本为 **K8s 巡检扩展**：ConfigMap / Secret（脱敏）/ Ingress 只读巡检。

### Added（新增）

- **配置与网络资源巡检**（工作负载页新增标签页）
  - `GET /api/k8s/configmaps`：ConfigMap 列表（键名与各值字节量，不含值）
  - `GET /api/k8s/secrets`：Secret 列表（**安全脱敏**：仅返回键名与类型，永不返回值）
  - `GET /api/k8s/ingresses`：Ingress 列表（Class / Hosts / TLS 状态）
  - 前端工作负载页新增「ConfigMap」（含 Secret 脱敏表）与「Ingress」标签页，支持命名空间过滤与搜索
  - 新增 `k8sClient.networkingApi()` 导出（NetworkingV1 API）

### Test（测试）

- 假 apiserver 全链路冒烟 6/6（configmaps 键名+字节量、secrets 脱敏断言、ingresses hosts/TLS、既有端点回归）；E2E 5/5；docs:check 86 图 0 缺失

## [1.7.0] - 2026-09-02

## [1.7.0] - 2026-09-02

本版本为 **Kubernetes Pod 终端**：Pod 详情页新增交互式 Web 终端（xterm.js + WebSocket exec），与 Docker 容器终端同款体验。

### Added（新增）

- **Pod 交互式终端**
  - 新增 `/ws/k8sterminal/:ns/:pod/:container` WebSocket：client-node Exec（TTY 模式）在 Pod 容器内启动 /bin/sh，双向数据桥接至前端 xterm.js
  - Pod 详情页「打开终端 / 关闭终端」按钮（管理员可见），多容器默认连接首个
  - 与 Docker 终端一致鉴权（登录且 operator/admin），错误信息友好化（Pod 不存在 / 容器未运行 / 无可用 shell / 集群不支持 exec）
  - 注：client-node 1.x Exec 暂不支持运行期 resize，终端尺寸由前端自适应

### Test（测试）

- WebSocket 冒烟：未认证升级 401 拒绝；E2E 5/5；docs:check 86 图 0 缺失


## [1.6.0] - 2026-09-02

本版本为 **Kubernetes 有限写操作（二期）**：扩缩容、滚动重启与删除 Pod 全链路接入既有高危操作审批流。

### Added（新增）

- **K8s 写操作（三个只读→可写端点）**
  - `POST /api/k8s/deployments/:ns/:name/scale`：调整 Deployment 副本数（0-500 整数校验）
  - `POST /api/k8s/deployments/:ns/:name/restart`：滚动重启（更新 restartedAt 注解触发滚动更新）
  - `DELETE /api/k8s/pods/:ns/:name`：删除 Pod（受 Deployment 管理的 Pod 自动重建）
- **高危操作审批流集成**：新增动作类型 `k8s.deployment.scale`（调整 K8s 副本数）、`k8s.deployment.restart`（重启 K8s Deployment）、`k8s.pod.delete`（删除 K8s Pod），与 Docker 动作共用审批中心、单号、多级签批与通知推送；支持在系统参数「高危操作审批流 → 两级审批动作」中将其升级为两级双签
- **RBAC 新增 Kubernetes 权限组**：`k8s.write`（扩缩容/滚动重启）、`k8s.delete`（删除 Pod）；内置 operator 角色默认含 `k8s.write`（启动时对存量 system 角色做增量合并，只增不删），删除 Pod 默认仅管理员
- **前端操作入口**（管理员可见）：工作负载页 Deployment 行「扩缩容 / 滚动重启」、Pod 详情页「删除 Pod」；非管理员操作被门禁拦截时提示已转审批并携带 AP- 单号

### Test（测试）

- 新增单测 k8sApproval.test.ts（4 例）：GATE_ACTIONS 注册、admin/operator/user 三类角色门禁判定（含 k8s.write 放行与 k8s.delete 拦截）、k8s 审批单提交与通过后执行器触发、rbac 增量合并；单测累计 **296/296**
- 假 apiserver 全链路冒烟 9/9：scale / restart / delete pod 写操作、非法副本数 400、只读端点回归
- E2E 5/5 无回归

## [1.5.0] - 2026-09-02

本版本为 **Kubernetes 只读巡检（一期）**：新增独立的 K8s 服务域，支持多集群概览、工作负载巡检、Pod 日志与实时指标，零 Docker 重构。

### Added（新增）

- **Kubernetes 服务域（只读）**
  - 新增 `server/src/k8s/k8sClient.ts`：kubeconfig 加载（`KUBECONFIG` 环境变量 > `~/.kube/config` > InCluster 三级优先）、多 context（多集群）列表与运行期切换、Quantity 解析（CPU 毫核 / KiB·MiB·GiB 二进制与 SI 十进制）
  - 新增 `/api/k8s/*` 只读端点：`status`（可用性与 context 列表）、`context`（切换）、`overview`（节点 + 资源计数 + metrics-server 资源占用）、`namespaces`、`pods`、`pods/:ns/:name`（详情）、`pods/:ns/:name/logs`（tail ≤ 2000 行）、`pods/:ns/:name/metrics`（实时快照）、`deployments`、`services`、`pvc`、`events`；全部需登录
  - 集群不可用（无 kubeconfig 且非 InCluster）时统一返回 **503 + 配置引导**，前端展示三选一启用说明
- **前端三个新页面**（侧栏新增「K8s 集群 / 工作负载 / K8s 事件」入口）
  - 集群概览（`/k8s`）：context 与命名空间切换器、节点/Pod/Service/PVC 统计卡、节点表（状态 / CPU / 内存占用条 / 版本 / IP）；未检测到 metrics-server 时自动降级提示
  - 工作负载（`/k8s/workloads`）：Pod / Deployment / Service / PVC 四标签页，命名空间过滤与关键字搜索；Pod 行可点击进入详情，CrashLoopBackOff 等容器级细化状态直接在列表展示
  - Pod 详情（`/k8s/pod/:ns/:name`）：基本信息、容器列表（就绪 / 重启 / 镜像）、日志（多容器切换 + tail 500 行）、CPU 与内存实时曲线（页面停留期间每 15 秒采样，不做后端留存）、相关事件
  - 集群事件（`/k8s/events`）：级别过滤（Warning/Normal）、命名空间过滤与关键字搜索

### Changed（调整）

- 依赖新增 `@kubernetes/client-node@^1.4.0`（纯 JS，无原生编译）
- 侧栏导航与路由注册新增 K8s 域三项（集群概览 `end` 精确匹配，避免子路由高亮冲突）

### Test（测试）

- 新增单测 k8sClient.test.ts（5 例）：KUBECONFIG 路径优先级、临时 kubeconfig 加载与 context 列表、运行期切换 context（未知 context 报错）、无效 kubeconfig 抛错、Quantity 解析（毫核 / 二进制 / SI）；单测累计 **292/292**
- API 冒烟：无集群环境 10/10（status 引导、7 端点 503 降级、未认证 401）；假 apiserver 全链路 18/18（概览计数、metrics-server 占用计算、Pod 列表 / 过滤 / 详情 / 日志 / 指标、Deployment / Service / PVC / 事件、context 切换）
- E2E 5/5 无回归

## [1.4.0] - 2026-09-01

本版本为 **平台化**：新增 OpenAPI 3.0 接口文档与「API 文档」页，便于二次开发与自动化对接。

### Added（新增）

- **OpenAPI 3.0 接口文档**
  - 新增 `GET /api/openapi.json`（需登录）：输出核心端点骨架（认证 / 监控 / 容器 / 镜像 / 卷 / 网络 / 编排 / 计划任务 / 审批 / 系统管理，共 34 个路径），含方法、说明、查询参数与 Bearer 鉴权声明
  - 新增「API 文档」页（`/api-docs`，侧栏入口）：按业务域分组渲染端点表，支持关键字搜索
- **PWA 与移动端适配确认**：manifest / Service Worker / 图标与 768px 响应式断点（侧栏抽屉、表格横滚、触控优化）在既有版本已落地，本版本完成核对与文档说明

### Test（测试）

- 新增单测 openapi.test.ts（2 例）：文档结构符合 OpenAPI 3.0（端点数 ≥ 30）、端条目含标签 / 鉴权 / 查询参数；单测累计 **287/287**
- 冒烟：未登录访问 401、登录后返回 34 路径的完整文档、manifest.webmanifest 存在于构建产物；E2E 5/5

## [1.3.0] - 2026-09-01

本版本为 **审批与协作**：多级审批链（两级双签）、审批单编号与超时前催办。

### Added（新增）

- **审批单编号**：所有审批记录生成唯一编号 `AP-YYYYMMDD-ID`，列表、通知推送与门禁响应统一携带，便于在工单系统中引用（存量记录惰性回填）
- **多级审批链**：系统参数「安全 → 两级审批动作」填入动作类型（如 `container.delete`），该动作的审批单需两级签批——第一级运维（operator）或管理员、**末级必须管理员**；审批中心展示「第 1/2 级」进度与完整签批轨迹（每级的决策人 / 时间 / 理由），每级通过均推送进度通知
- **超时前催办**：待审批达到 TTL 的 3/4 时自动推送一次催办通知（每条记录仅一次），并由定时器主动执行过期清理（此前为查询时惰性过期）

### Changed（调整）

- 审批批准/拒绝接口从「仅管理员」放宽为「运维或管理员」：末级签批仍强制管理员（decideApproval 内校验，operator 触达末级返回 403）
- 门禁转审批的 202 响应新增 `ticketNo` 字段

### Test（测试）

- 新增单测 approvalChain.test.ts（4 例）：两级动作级数固化与编号生成、operator 一级推进 + 末级 403 + 管理员终批轨迹、单级动作 operator 403、超时前提醒一次性触发；单测累计 **285/285**
- API 冒烟 14/14：门禁转审批带单号、operator 一级推进 / 末级 403 / 管理员终批、列表级数进度、配置恢复与清理；E2E 5/5

## [1.2.0] - 2026-09-01

本版本为 **可观测性深化**：指标小时级聚合支撑 30/90 天长周期曲线，Trivy 漏洞定时扫描与新增高危推送。

### Added（新增）

- **指标长周期留存**
  - 新增 `metrics_hourly` 小时级聚合表：对主机（host_metrics）与容器（container_metrics）采样做小时级 rollup（CPU 均值/峰值、内存均值/峰值、磁盘、网络增量），保留 **90 天**
  - 总览「资源监控」与容器详情「资源曲线」时间窗扩展 **30 天 / 90 天**（30d 每 6 小时一点、90d 每 12 小时一点）
  - 新增聚合器随服务自动启动，每 15 分钟补聚合上一小时数据（幂等，停机不漏）
- **Trivy 漏洞定时扫描**
  - 新增计划任务类型 `vulnScan`：按显式镜像列表（CSV）扫描，缺省自动扫描本地镜像前 N 个（默认 20，可配置）
  - 扫描结果写入 `vuln_scans` 历史表（含每轮 CVE id 快照，上限 500 条）
  - 与上一次同镜像记录按 CVE id **差集对比**，新增 Critical / High 时自动推送告警到全部启用通知渠道
  - 镜像详情页新增「扫描历史（定时任务）」对比表（时间 / 各级别计数 / 较上次新增高危）
  - 新增 `GET /api/images/vuln-history`（全部或按镜像查询扫描历史）

### Changed（调整）

- 主机/容器历史查询范围白名单扩展 30d/90d；原始采样表保留策略不变（7 天）

### Test（测试）

- 新增单测 metricsVuln.test.ts（4 例）：host/container 小时聚合 avg-max-增量求和、CVE 差集对比（含首扫与重复条目去重）、扫描留存与历史查询；单测累计 **281/281**
- API 冒烟：10m→90d 六档主机历史、容器 30d 曲线、漏洞历史接口全部通过；E2E 5/5

## [1.1.0] - 2026-09-01

本版本为 **安全加固**：2FA 双因素认证、会话管理、IP 白名单与密码策略，补齐账号安全体系。

### Added（新增）

- **2FA 双因素认证（TOTP，RFC 6238 零依赖实现）**
  - 登录二步验证：密码通过后签发 2 分钟单次有效票据，需输入认证器 6 位验证码方可登录
  - 设置页自助管理：生成密钥（Base32 + otpauth URI）→ 首枚验证码确认启用；关闭需当前验证码确认
  - 密钥以加密形式存储于 users 表
- **会话管理**
  - 在线会话列表（会话 ID / 用户 / IP / 登录时间），当前会话标记；支持撤销单条与撤销其他全部会话
  - 新增配置 `auth.maxSessionsPerUser`：单用户并发会话上限，超出自动淘汰最早会话（0 = 不限制）
- **IP 白名单（全局 + 按用户）**
  - 全局白名单（配置 `security.ipAllowlist`）与按用户白名单（用户管理内联编辑），支持 IPv4 CIDR 与精确 IP（IPv6 精确串）
  - 用户级白名单优先于全局；登录与每次 API 请求双重校验，命中失败返回 403
- **密码策略**
  - `security.passwordMinLength` 最小长度、`security.passwordRequireComplex` 须含大小写与数字、`security.passwordExpiryDays` 有效期（过期登录强制改密）
  - 新建用户与修改密码统一执行策略

### Changed（调整）

- 登录页新增 2FA 验证码输入视图；设置页用户新增「IP 白名单」列与「在线会话」「两步验证（2FA）」区块
- users 表迁移新增 totp_secret / totp_enabled / ip_allowlist / pwd_changed_at 列（自动迁移，无需手工干预）

### Test（测试）

- 新增单测 security2.test.ts（7 例）：TOTP RFC 6238 向量、Base32 往返、CIDR 匹配、白名单解析、密码策略、密码过期、会话列表/上限/撤销；单测累计 **277/277**
- API 冒烟 20/20：2FA 启停与登录流、会话列表/撤销、用户级与全局白名单 403、密码策略拦截
- typecheck + vite build 通过；E2E 5/5（默认中文零回归）

## [1.0.0] - 2026-08-31

本版本为 **i18n 全覆盖收官**：剩余 20 个页面全部完成迁移，全部界面实现中英文完整覆盖。

### Added（新增）

- **i18n 英文包收官批（全覆盖）**
  - 文件管理、操作日志、云端备份、存储统计（约 254 行）
  - Swarm、工具箱、构建镜像、事件流（约 193 行）
  - 容器模板、防火墙、安全基线、端口地图（约 134 行）
  - 日志聚合、镜像 GC、健康体检、网络拓扑、宿主机终端（约 94 行）
  - AI 助手（对话 / 知识库 / 用量 / 巡检 / 模型管理，约 250 行）、镜像中心（约 123 行）、站点反代（约 100 行）、帮助中心全量文案（含 FAQ 英译，约 112 条）
  - 英文包新增约 1057 词条，累计 **2704 词条**；全部 27 个页面完成迁移，无回退页面

### Changed（调整）

- 界面语言中英文覆盖从「核心层 + 高频页」升级为 **100% 全覆盖**；未收录文案仍自动回退中文

### Test（测试）

- typecheck + vite build 通过；E2E 冒烟 5/5（默认中文零回归）；语言切换冒烟通过；终检脚本确认全部页面已迁移（0 个 UNMIGRATED）

## [0.9.0] - 2026-08-31

本版本聚焦 **i18n 英文包第四批 + 审批记录导出**：网络 / 引擎 / 镜像详情 / 备份 / 宿主机文件完成英文覆盖，审批记录支持按状态导出 CSV。

### Added（新增）

- **i18n 英文包第四批（覆盖推进）**
  - 网络管理（列表 / 拓扑 / 连接容器 / 清理，约 80 行）
  - 引擎管理（多引擎端点 / 跨引擎总览 / 镜像分发，约 84 行）
  - 镜像详情（层分析 / 漏洞扫描 / 构建历史 / 标签管理，约 67 行）
  - 备份恢复（面板数据库 / 数据卷 / Compose / 站点配置备份，约 62 行）
  - 宿主机文件（浏览 / 上传 / 编辑 / 打包下载，约 74 行）
  - 英文包新增约 307 词条，累计 1800+ 词条
- **审批记录导出**
  - 新增 `GET /api/approvals/export?status=`：按状态过滤导出审批记录 CSV（UTF-8 BOM，Excel 友好；管理员导出全部，其他用户仅导出自己提交的，不受列表 200 条截断限制）
  - 审批中心工具栏新增「导出CSV」按钮（跟随当前状态筛选）

### Test（测试）

- typecheck + vite build 通过；E2E 冒烟 5/5（默认中文零回归）；语言切换冒烟通过
- 审批统计单测扩展 CSV 渲染（表头 / 字段转义 / 状态过滤），服务端单测 270/270

## [0.8.0] - 2026-08-31

本版本聚焦 **i18n 英文包第三批 + 审批统计**：应用商店 / 编排 / 卷 / 审批中心完成英文覆盖，审批中心新增近 30 天统计看板。

### Added（新增）

- **i18n 英文包第三批（长尾高频页覆盖）**
  - 应用商店（内置应用 / 自定义应用 / 安装卸载 / 升级，约 150 行）
  - Compose 编排（项目管理 / 服务结构 / 日志 / 生命周期操作，约 141 行）
  - 数据卷管理（卷列表 / 详情 / 克隆 / 备份 / 导出 / 清理，约 111 行）
  - 容器启动编排（依赖配置 / 一键编排 / 历史与重试，约 101 行）
  - 审批中心（列表 / 筛选 / 批准拒绝 / 撤销与统计卡片，约 59 行）
  - 英文包新增约 315 词条，累计 1500+ 词条
- **审批统计**
  - 新增 `GET /api/approvals/stats?days=N`：近 N 天按状态汇总（待审批 / 已批准 / 已拒绝 / 已撤销）、按动作类型分布、按提交人分布（Top 10）、执行质量（执行成功 / 失败）
  - 审批中心新增「审批统计」卡片（管理员可见）：执行成功 / 失败速览 + 按动作类型分布表 + 提交人 Top 榜

### Test（测试）

- typecheck + vite build 通过；E2E 冒烟 5/5（默认中文零回归）；语言切换冒烟通过
- 新增 `test/approvalStats.test.ts` 审批统计单测（4 例：状态汇总与执行质量 / 动作分布 / 提交人分布 / 天数窗口），服务端单测 269/269

## [0.7.0] - 2026-08-31

本版本聚焦 **i18n 英文包第二批 + 渠道送达率统计**：高频详情与运维页完成英文覆盖，告警推送链路新增渠道级送达结果留痕与统计。

### Added（新增）

- **i18n 英文包第二批（高频页覆盖）**
  - 容器详情页（基本信息 / 日志 / 终端 / 资源监控 / 文件管理 / 各编辑弹窗与确认文案，约 460 行）
  - 通知中心（告警规则 / 推送路由 / 自愈 / 渠道 / 告警记录与全部弹窗，约 374 行）
  - 数据库管理（实例登记 / 库表浏览 / SQL 查询 / Redis 键 / 数据备份，约 302 行）
  - 计划任务（任务列表 / cron 可视化编辑 / 执行历史 / 配置弹窗，约 294 行）
  - 英文包新增约 630 词条，累计 1200+ 词条；模块级字典统一"裸值存储 + 调用处翻译"
- **渠道送达率统计**
  - 新增 `notify_push_log` 推送日志表：每次渠道级推送（告警 / 恢复 / 自愈 / 审批 / AI 诊断 / 周报 / 测试推送）的送达结果留痕，保留最近 5000 条
  - 新增 `GET /api/notifications/push-stats?days=N`：近 N 天按渠道聚合成功 / 失败 / 成功率、最近成功与失败时间、最近失败明细（10 条）
  - 告警中心新增「送达率统计」卡片：按渠道展示推送成功 / 失败 / 送达率 / 最近成功与失败时间，附最近失败明细

### Test（测试）

- typecheck + vite build 通过；E2E 冒烟 5/5（默认中文零回归）；语言切换冒烟通过
- 新增 `test/pushStats.test.ts` 送达率统计单测（4 例：汇总 / 渠道聚合 / 失败明细排序 / 天数窗口过滤），服务端单测 265/265

## [0.6.0] - 2026-08-31

本版本聚焦 **国际化（i18n）骨架与英文包首批覆盖**：零依赖 i18n 框架落地，核心层界面支持中英文切换。

### Added（新增）

- **i18n 骨架（零依赖，gettext 风格）**
  - 新增 `web/src/i18n/`：LanguageContext + `t()` 翻译函数 + zh/en 字典，中文原文即 key，未收录键自动回退中文原文
  - 设置页新增「界面语言」切换（中文 / English），语言偏好持久化到 localStorage（`dm.lang`），切换即时生效
  - 新增独立于 React 树的 `translateNow()`，供工具函数等非组件上下文使用
- **英文包首批覆盖（核心层）**
  - 全局框架：侧边栏导航（37 项）、顶栏、全局搜索、退出登录
  - 登录页、公共组件（Toast / Modal / ConfirmDialog / Empty / Loading / 全局搜索）
  - 高频页面：总览、容器、镜像、设置（含新增用户 / 角色管理 / 备份恢复 / 系统参数 / 关于）
  - 英文包共收录 580+ 词条（含插值变量 {{n}}/{{name}} 等）

### Changed（调整）

- 默认界面语言保持中文（zh），现有用户升级零感知；切换英文后未迁移页面自动回退中文显示
- 页面标题 `document.documentElement.lang` 随语言切换同步更新

### Test（测试）

- typecheck + vite build 通过；E2E 冒烟 5/5（默认中文零回归）
- 新增语言切换冒烟验证：en 登录页/侧边栏英文渲染、zh 回退正常

## [0.5.0] - 2026-08-29

本版本聚焦 **运维自动化 + 权限体验闭环** 三大主线：容器自愈、通知模板变量、RBAC 体验闭环。

### Added（新增）

- **容器自愈**
  - 按容器名配置自愈规则：监听「健康检查失败（unhealthy）」或「容器退出/死亡（exited/dead）」，命中后自动重启（restart）或启动（start）
  - 规则级冷却期（10-86400 秒，默认 300 秒）防重：同一规则冷却窗口内不重复触发，避免反复重启刷屏
  - 全自动但全程可审计：每次触发写入告警记录（类型 = 自愈），成功推 recovery、失败推 danger，走多渠道路由策略
  - 告警中心新增「容器自愈」卡片：规则 CRUD、启停、最近触发时间、立即巡检
  - 后台 10 秒周期巡检（与告警检测同节奏）；新增 selfheal.manage 权限键管控规则维护
- **通知模板变量**
  - 通知渠道新增「消息模板」：支持 {{level}}、{{message}}、{{time}}、{{channel}} 四个变量，未知变量原样保留
  - 模板为空保持默认文案（升级无感）；告警/恢复/自愈/审批/AI 诊断等全部推送链路按渠道渲染
- **RBAC 体验闭环**
  - 角色与自愈规则纳入配置导入/导出：自定义角色按名称 upsert（内置角色跳过、权限键过滤），换机迁移不再丢失
  - 镜像/卷/网络/Compose 四页操作按钮按权限键显隐（拉取/写入/删除/清理/编排），自定义角色不再看到「点了就 403」的按钮
  - 容器终端 WebSocket 判定统一为权限化：自定义角色持有 containers.run 权限即可使用终端，auditor/user 仍被拒绝
  - /api/auth/me 返回权限数组，前端权限判定以服务端为权威

### Changed（调整）

- 权限目录新增「自愈」分组（selfheal.manage），共 14 项
- WebSocket upgrade 鉴权的 operator 判定由角色名硬编码改为权限数组判定，行为对内置角色完全兼容

### Test（测试）

- 新增通知模板单测（9 例：变量替换/未知保留/空模板透传/渠道回环/限长 400）
- 新增容器自愈单测（9 例：命中判定/冷却期/CRUD/去重 409/非法入参 400）
- 全量单测 261/261 通过

## [0.4.0] - 2026-08-29

本版本聚焦 **通知路由 + 协作治理** 三大主线：告警多渠道路由、审批门禁扩展、自定义角色 RBAC。

### Added（新增）

- **告警多渠道路由**
  - 推送策略三种可选：仅首个启用渠道（兼容旧版默认）/ 全部启用渠道 / 按级别路由
  - 按级别路由：warn / danger / recovery 三级各自勾选目标渠道（未勾选时回退首个启用渠道），danger 走 Telegram、warn 走钉钉等分流场景落地
  - 聚合器按级别分桶：同级别同窗口合并为一条摘要，不同级别不混合发送
  - 告警中心新增「推送路由」卡片；告警记录的推送渠道字段记录实际目标渠道集合
- **审批门禁扩展**
  - 新增 4 类可审批动作：停止编排项目（compose down）、批量删除镜像、清理悬空镜像、清理未使用卷；既有删除容器/镜像/卷、清理网络、修复容器配置全部继续可用
  - 卷删除、卷/网络/镜像 prune、compose down 等入口由管理员专属放宽为「登录即可发起」：审批流关闭时行为不变（403），开启后非管理员可提交审批、管理员批准后自动执行
  - 审批中心支持新动作类型的展示与批量处理；卷/镜像/网络/编排页对「已提交审批」状态给出即时提示
- **自定义角色 RBAC**
  - 新增角色管理（设置页）：13 项资源域权限目录（容器/镜像/卷/网络/编排 5 组），可创建任意自定义角色并按白名单授权
  - 内置角色 admin（全部权限）/ operator（默认生命周期权限，可调）/ user / auditor（只读）锁定防误改；使用中的角色禁止删除
  - 权限中间件覆盖 23 处资源路由；用户管理、系统设置、引擎切换等系统级操作保持管理员专属
  - 与审批门禁联动：角色持有某操作权限即直接执行，未持有则走审批流（开启时）或 403
  - 用户创建的角色下拉动态列出全部角色（含自定义）

### Changed（调整）

- 告警推送的渠道解析由「首个启用渠道」硬编码升级为可配置策略，默认值保持旧行为，升级无感
- CI 的 checkout / setup-node 升级至 v5，消除 Node 20 弃用告警

### Test（测试）

- 新增多渠道路由解析单测（6 例：三种策略 + 停用过滤 + 空表回退 + 非法模式）
- 新增审批门禁单测（5 例：动作注册/管理员放行/403/202/未知动作）
- 新增 RBAC 单测（6 例：内置种子/权限判定/CRUD 守卫/中间件/审批联动）

## [0.3.0] - 2026-08-29

本版本聚焦 **可观测性深化 + 通知生态**，补齐告警防抖闭环与主流 IM 渠道。

### Added（新增）

- **告警持续时间窗口化**
  - 宿主级资源告警（CPU/内存/磁盘/GPU/带宽）与容器级 CPU/内存规则支持「连续周期」配置：连续 N 个采样周期超阈值才触发（每周期约 10 秒，1-120，默认 1 = 立即告警），有效过滤瞬时毛刺
  - 防抖闭环补齐最后一环：瞬时毛刺 → 持续窗口化（本次）→ 推送聚合 → 静默时段 → 工作时段
  - 告警规则编辑弹窗新增「连续周期」输入项
- **通知渠道扩展**
  - 新增 Telegram Bot / 企业微信群机器人 / Slack Incoming Webhook 三种通知渠道，与既有钉钉/飞书同构：Bot Token 等敏感字段加密存储、编辑时留空保留原密文
  - 测试推送、告警推送、推送聚合、AI 诊断对全部新渠道生效
- **告警规则导入/导出**
  - 面板配置导出/导入补齐告警规则的「连续周期」字段与容器级规则的 CPU/内存阈值，迁移场景不再丢失防抖配置

### Fixed（修复）

- 面板配置导出的「容器级告警规则」遗漏 CPU/内存阈值字段，导入后回退默认值（现完整导出）

### Test（测试）

- 新增告警持续时间窗口状态机单测（触发窗口/升级/恢复/清零，14 例）
- 新增 Telegram / 企业微信 / Slack 渠道校验与加解密回环单测（6 例）

## [0.2.0] - 2026-08-28

本版本聚焦 **数据治理、安全基线深化、AI 深化与审批体验** 四条主线。

### Added（新增）

- **数据保留自动清理**（P1）
  - 操作日志、AI 用量明细、AI 巡检记录按保留天数自动清理（设置中心可配，0 = 永久保留）
  - 配置导入事务 `BEGIN IMMEDIATE` 加固，消除与惰性清理的并发竞争
- **安全基线定时扫描**（P2）
  - 新计划任务类型「安全基线扫描」：定时扫描 + 违规快照对比，新增违规自动推送通知渠道
  - 可配置告警严重度下限（danger/warn/info）与「仅新增违规时告警」
- **AI 高危操作转审批单**（P3）
  - 审批流开启时，AI 建议的删除容器/镜像操作自动转管理员审批单（AI 操作进入「待管理员审批」状态）
  - 管理员批准执行后结果自动回写 AI 操作记录（闭环）
- **告警自动 AI 诊断开关**（P3）
  - 新设置 `alerts.aiDiagnosis`（默认开启）；容器级 danger 告警同样触发 AI 诊断（此前仅宿主级）
- **审批批量处理**（P4）
  - 批量批准/拒绝接口与审批中心多选 UI（单批 ≤ 50 条，逐条顺序执行）
  - 拒绝理由必填（单条与批量，后端 400 校验 + 前端理由弹窗）
  - 操作日志新增「审批」「AI 操作」目标类型筛选
- **安全基线违规在线一键修复**（基线三期）
  - 内存 / CPU / 重启策略三类违规经 Container Update API 在线修复，修复后自动复检
  - 修复动作走审批门禁（`container.fix`），非管理员提交转审批单
- **面板数据库备份**
  - 面板自身 SQLite 数据库一致性快照（`VACUUM INTO`，不停服）
  - 新计划任务类型「数据库备份」支持定时自动备份；保留份数可配（默认 7 份）
  - 一键恢复（含 SQLite 格式校验与 `quick_check` 完整性校验）、下载、删除
- **E2E 冒烟测试**
  - e2e/ 独立 Playwright 工程：登录 → 总览 → 容器 → 审批中心 → 安全基线 4 条主链路
- **告警推送窗口聚合（防风暴）**
  - 窗口内（`alerts.pushAggWindowSec`，默认 60s，0=关闭）多条 warn/danger 推送合并为一条摘要（最多 5 条原文 + 总数）
  - recovery 与窗口关闭时逐条即时推送；被聚合记录 push_status='aggregated'；聚合推送不触发 AI 诊断
- **E2E 计划任务全链路**
  - 新建数据库备份任务 → 立即执行 → 执行历史 → 删除清理

### Fixed（修复）

- 计划任务手动执行未走调度器注册表：AI 巡检 / AI 周报 / 基线扫描等类型手动执行报「未注册处理器」
- `web/src/types/index.ts` 中文注释乱码（编码损坏，仅注释受影响）
- 计划任务类型选项补齐缺失的「镜像清理」（imageGc）
- 面板数据库恢复缺少存在性校验（不存在时裸 ENOENT → 500，现统一 404）
- 镜像搜索用例 Docker Hub 可达性探测：离线/网络受限环境自动跳过，消除假失败

### Changed（变更）

- SQLite `busy_timeout` 5s → 15s（服务器连接）；配置导入事务隔离级别提升
- 测试隔离加固：configTransfer 导入测试注入无冲突键；保留天数用例自愈重试
- 并行测试抖动根治：审批门禁用例并入 approvals 文件（消除 approvals.enabled 跨文件互踩）；数据库恢复改隔离单测
- **.rpm 构建环境迁移 AlmaLinux 9**（CentOS 7 glibc 2.17 无法运行 Node 22）；修复容器内 tsc 缺 @types、rpmbuild `--buildarch` 无效参数、curl 与 curl-minimal 冲突、install.sh 未进镜像
- Windows 上可用 `build-deb-win.ps1` / `build-rpm-win.ps1` 直接驱动 Linux 打包（无需 WSL Docker 集成）
- 安装包版本号单一来源：从根 package.json 自动注入 NSIS（/DAPP_VERSION）
- 审批门禁用例并入 approvals 测试文件；test:api 清单补齐新增测试文件（628 项）

### Fixed（修复）

- 手册在 GitHub 上大量图片无法显示：`docs/*.md` 中图片相对路径改为 `../images/`（GitHub 按文档目录解析），删除 `docs/images/` 下 33 张过时副本，统一引用根 `images/`
- `docs:check` 校验脚本升级为 GitHub 同款相对路径解析规则

### Test（测试）

- 单测 206/206、API 集成测试 628/628（含可达性探测后实际全绿）
- E2E 冒烟 5/5（Playwright，浏览器建议经 npmmirror 镜像安装）
- 全量回归与互踩对连跑验证：3×18/18、3×63/63
- 新增 GitHub Actions 测试工作流（Test CI）：单元测试 / API 集成 / 文档图片死链校验三并行 job
- Test CI 补前端质量门（web `tsc -b` + server `tsc --noEmit` 类型检查 job）与 E2E Playwright 冒烟 job（9528 后端 + 自动拉起 vite）
- 新增 `npm run docs:check` 跨平台文档图片校验脚本（`scripts/check-doc-images.js`）

## [0.1.0] - 初始版本

- 容器 / 镜像 / 卷 / 网络管理、Compose 编排、应用商店、构建可视化
- 多引擎管理、监控告警、通知渠道、操作日志、用户与 RBAC
- AI 智能助手（对话 / 巡检 / 周报 / 知识库）、云备份、站点反代、防火墙等
